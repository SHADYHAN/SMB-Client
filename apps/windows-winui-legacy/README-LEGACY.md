# Windows WinUI Legacy Client

This directory contains the previous WinUI 3 Windows client implementation.

It is retained for:

- Feature reference
- Behavior comparison
- Temporary migration source
- Regression examples

Do not use this directory as the active Windows client line. The active Windows client is `apps/windows-tray`.
