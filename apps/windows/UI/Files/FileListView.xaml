<UserControl x:Class="Rynat.WindowsClient.UI.Files.FileListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource PanelBackgroundBrush}"
            Padding="12,14">
        <DockPanel>
            <Grid DockPanel.Dock="Top"
                  Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="180" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding PathTitle}"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center" />
                <TextBlock Grid.Column="1"
                           Text="正在加载..."
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,0,12,0"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBox Grid.Column="2"
                         MinHeight="30"
                         Padding="8,4"
                         VerticalContentAlignment="Center"
                         ToolTip="搜索当前目录" />
            </Grid>

            <ListView ItemsSource="{Binding Items}"
                      SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      MouseDoubleClick="ListView_OnMouseDoubleClick"
                      SelectionChanged="ListView_OnSelectionChanged"
                      BorderThickness="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="名称" Width="320">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Icon}"
                                                   Width="24" />
                                        <TextBlock Text="{Binding Name}" />
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="类型"
                                        Width="110"
                                        DisplayMemberBinding="{Binding Type}" />
                        <GridViewColumn Header="大小"
                                        Width="100"
                                        DisplayMemberBinding="{Binding Size}" />
                        <GridViewColumn Header="修改时间"
                                        Width="160"
                                        DisplayMemberBinding="{Binding ModifiedAt}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </DockPanel>
    </Border>
</UserControl>
