<UserControl x:Class="Rynat.WindowsClient.UI.Login.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Border Width="380"
                Padding="30"
                Background="{StaticResource PanelBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="18"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel>
                <Image Source="/Assets/RYNATLogo.png"
                       Width="58"
                       Height="58"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,14" />
                <TextBlock Text="rynat 共享网盘"
                           FontSize="22"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,24" />

                <TextBlock Text="服务器"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,0,0,6" />
                <ComboBox ItemsSource="{Binding ServerProfiles}"
                          SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                          DisplayMemberPath="DisplayName"
                          MinHeight="34"
                          Margin="0,0,0,8" />
                <TextBox Text="{Binding ServerHost, UpdateSourceTrigger=PropertyChanged}"
                         KeyDown="LoginInput_OnKeyDown"
                         Margin="0,0,0,14" />

                <TextBlock Text="用户名"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,0,0,6" />
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         KeyDown="LoginInput_OnKeyDown"
                         Margin="0,0,0,14" />

                <TextBlock Text="密码"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,0,0,6" />
                <PasswordBox x:Name="PasswordInput"
                             PasswordChanged="PasswordInput_OnPasswordChanged"
                             KeyDown="LoginInput_OnKeyDown"
                             Margin="0,0,0,12" />

                <StackPanel Orientation="Horizontal"
                            Margin="0,0,0,18">
                    <CheckBox Content="记住密码"
                              IsChecked="{Binding RememberPassword, Mode=TwoWay}"
                              VerticalAlignment="Center" />
                    <CheckBox Content="自动登录"
                              IsChecked="{Binding AutoLogin, Mode=TwoWay}"
                              VerticalAlignment="Center"
                              Margin="18,0,0,0" />
                </StackPanel>

                <Button Content="登录"
                        Command="{Binding LoginCommand}"
                        IsDefault="True"
                        Height="38" />
                <TextBlock Text="{Binding Message}"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0,16,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
